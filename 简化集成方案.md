# Thresh 简化集成方案

## 当前问题
Flutter 3.7.0 与 Gradle 8.9 存在API兼容性问题，导致构建失败。

## 推荐解决方案

### 方案一：独立Flutter应用 + Intent通信

1. **创建独立的Thresh Flutter应用**
```bash
flutter create thresh_app
cd thresh_app
# 在pubspec.yaml中添加thresh依赖
```

2. **主应用通过Intent启动Flutter应用**
```kotlin
// 在MainActivity中添加
private fun launchThreshApp() {
    val intent = packageManager.getLaunchIntentForPackage("com.example.thresh_app")
    if (intent != null) {
        startActivity(intent)
    } else {
        // 提示用户安装Thresh应用
        Toast.makeText(this, "请先安装Thresh应用", Toast.LENGTH_SHORT).show()
    }
}
```

### 方案二：降级Gradle版本（兼容性方案）

1. **修改gradle-wrapper.properties**
```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-7.6-bin.zip
```

2. **修改build.gradle.kts中的AGP版本**
```kotlin
plugins {
    id("com.android.application") version "7.4.2"
    // 其他插件...
}
```

### 方案三：使用WebView + H5（临时方案）

在现有应用中集成WebView，加载Thresh生成的H5页面。

## 推荐采用方案一

**优势:**
- ✅ 避免版本兼容性问题
- ✅ 独立部署和更新
- ✅ 开发和调试简单
- ✅ 不影响主应用稳定性

**实施步骤:**
1. 创建独立的Flutter Thresh应用
2. 在主应用中添加启动按钮
3. 通过数据传递实现两个应用间通信

这样既能体验Thresh的动态化能力，又避免了复杂的版本兼容性问题。 